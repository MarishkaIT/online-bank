yaml:
  duplicate-key: merge

spring:
  application:
    name: online-bank

  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: pass
    url: jdbc:postgresql://localhost:5432/db_bank

  liquibase:
    change-log: /db/changelog/db.changelog-1.0.sql

